import * as React from "react";
import { Text, Button } from "react-native";
import ScreenContainer from "./ScreenContainer";

function Profile(props: React.PropsWithChildren<{}>) {
  return (
    <ScreenContainer>
      <Text>Loading...</Text>
    </ScreenContainer>
  );
}

export default Profile;
