import * as React from "react";
import { Text, Button } from "react-native";
import ScreenContainer from "./ScreenContainer";

function Search2(props: React.PropsWithChildren<{}>) {
  return (
    <ScreenContainer>
      <Text>Search2 Screen</Text>
    </ScreenContainer>
  );
}

export default Search2;
